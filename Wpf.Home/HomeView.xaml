<UserControl x:Class="Wpf.Home.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf.Home"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="{Binding TestText}" FontSize="40" FontWeight="Bold" Margin="10 0 0 0"/>

        <Rectangle Fill="{Binding RandomColor}" StrokeThickness="1" HorizontalAlignment="Left" VerticalAlignment="Top" Width="40" Height="20" Margin="10,60,0,0"/>

    </Grid>
</UserControl>
