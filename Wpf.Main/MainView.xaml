<UserControl x:Class="Wpf.Main.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf.Main" 
             xmlns:datas="clr-namespace:Wpf.Main.Datas"
             xmlns:units="clr-namespace:Wpf.Assistance.UI.Units;assembly=Wpf.Assistance"
             xmlns:converters="clr-namespace:Wpf.Assistance.Local.Converters;assembly=Wpf.Assistance"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:EnumToBooleanConverter x:Key="enumToBooleanConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal" Margin="10">
                <units:ToggleRadioButton Content="Home" Margin="0 0 20 0" GroupName="Menu" IsChecked="{Binding MenuModel.MenuType, ConverterParameter={x:Static datas:MenuTypes.Home}, Converter={StaticResource enumToBooleanConverter}}" VerticalAlignment="Center"/>
                <units:ToggleRadioButton Content="Datas" Margin="0 0 20 0" GroupName="Menu" IsChecked="{Binding MenuModel.MenuType, ConverterParameter={x:Static datas:MenuTypes.Datas}, Converter={StaticResource enumToBooleanConverter}}" VerticalAlignment="Center"/>
                <units:ToggleRadioButton Content="Remark" Margin="0 0 20 0" GroupName="Menu" IsChecked="{Binding MenuModel.MenuType, ConverterParameter={x:Static datas:MenuTypes.Remark}, Converter={StaticResource enumToBooleanConverter}}" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Grid Background="#252526" Grid.Row="1">
            <ContentControl Grid.Row="1" Content="{Binding ContentView}"/>
        </Grid>
    </Grid>
</UserControl>
