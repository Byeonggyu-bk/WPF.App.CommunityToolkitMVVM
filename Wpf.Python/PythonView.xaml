<UserControl x:Class="Wpf.Python.PythonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf.Python"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="{Binding TestText}" FontSize="40" FontWeight="Bold" Margin="10 0 0 0"/>

        <Border BorderBrush="AliceBlue" BorderThickness="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0 60 0 0" Padding="0 0 10 10">
            <Grid>
                <TextBlock Text="※ Python 기반의 간단한 테스트 자동화 엔진" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Top" Height="26" Margin="145,17,0,0" />
                <Button Command="{Binding StartFuncCommand}" Content="실 행" HorizontalAlignment="Left" VerticalAlignment="Top" Width="130" Height="30" Margin="10,10,0,0"/>
                <TextBox Text="{Binding LogText}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="500" Height="180" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="10,45,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
